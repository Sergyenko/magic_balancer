MagicBalancer

To play with magic balancer try:

        rake console

and then:

        balancer = MagicBalancer.new("google.com")
        balancer.get_ip

or

         MagicBalancer.get_ip("google.com")
